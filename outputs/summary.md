# Sweep Summary

This file is generated by `python sweep.py` and summarizes the accuracy-sparsity trade-off across lambda values.

The current checked-in table is a quick validation result rather than a full-dataset benchmark. The latest curated sweep used `15` epochs, train subset `4096`, test subset `1000`, gate init mean `0.5`, and gate-score learning-rate multiplier `15`.

| Lambda | Test Accuracy | Sparsity Level (%) |
| --- | ---: | ---: |
| 2e-04 | 40.90 | 93.84 |
| 5e-04 | 37.10 | 98.09 |
| 1e-03 | 34.20 | 99.33 |

The table reports final-epoch accuracy and final-epoch thresholded sparsity. To keep the repository lightweight, the checked-in artifacts focus on the final sparse endpoint rather than also keeping the larger checkpoint and duplicate evaluation outputs.

The representative checked-in files are `outputs/plots/final_sparse_gate_hist.png` and `outputs/metrics/final_sparse_summary.json`. Those two files correspond to the final epoch of the `lambda=2e-4` run, so they align exactly with the first row in the table.
